# Hashicorp Vault with camel

## Usage

* Start Vault with the provided docker-compose file.
* Login to the GUI and create a secret named database with `url` property
* Start the casmel route and see the secret printed on the console




