- route:
    from:
      uri: file:source
      steps:
        - split:
            expression:
              xtokenize:
                expression: /company/address
                mode: u
                trim: true
        - log: ${body}
